## create directories needed for dots

cd "$HOME/.config"

mkdir "waybar"
mkdir "wal"
mkdir "wal/templates"
mkdir "waybar"
mkdir "rofi" 
mkdir "kitty"
mkdir "fastfetch"

ls -l
cd "$HOME/"
mkdir "$HOME/Pictures"


cd "$HOME/dots"

ls -l
sleep 1